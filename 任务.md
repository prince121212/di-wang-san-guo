# 《帝王三国》自研辅助 App / 服务端工具链任务进度表

## 项目终极目标

做一个《帝王三国》的自研辅助 App / 服务端工具链，通过抓包和逆向协议，把游戏账号的真实接口数据解析出来，并在自研辅助 App 里展示、管理和调用接口来自动操作游戏。

重点场景示例：刷黄任务。

玩家有若干将领，将领可以带领不同的兵，分配完成以后，可以派特定将领去剿灭山贼。流程需要先从地图上找山贼，再筛选，直到找到符合筛选条件的山贼，然后指派特定将领出征。

## 当前已有资产

- `/Users/huangchangwei/Desktop/gitSpaceC/Toy/帝王三国/小黄点辅助.apk`
  - 之前做过的一款辅助 App。
  - 源码丢失，目前只能作为功能和交互参考。
  - 直接逆向它无法完整找回源码，只能借鉴实现方式和功能点。

- `/Users/huangchangwei/Desktop/gitSpaceC/Toy/帝王三国/reverse_case_小黄点辅助`
  - 小黄点辅助相关逆向分析资产。

- `/Users/huangchangwei/Desktop/gitSpaceC/Toy/帝王三国/自研辅助源码`
  - 当前正在开发的自研辅助源码。
  - 最终产品将由这份源码构建出来。

- `/Users/huangchangwei/Desktop/gitSpaceC/Toy/帝王三国/三国·帝王联盟1.66.apk`
  - 游戏本体。

## 测试环境

- 手机已通过数据线连接电脑。
- 手机上已经安装最新构建出来的自研 App。
- 可随时进行真机实测。
- 测试账号：`1608601`
- 测试区服：`351`
- 说明：账号密码不写入文档、不写入代码仓库、不写入日志，后续只在本地加密配置或手机端手动输入。

---

# 总体原则

1. **每一步必须真机可验收**  
   不是“接口猜对了”，而是手机上能看到真实数据，或者完成真实游戏操作。

2. **先只读，后操作**  
   先把角色、将领、军队、宝物、军情等只读数据跑通，再做配兵、刷黄、打矿等写操作。

3. **每个接口留证据**  
   请求参数、响应样例、字段解释、App 页面截图、测试时间、账号区服都要能追溯。

4. **刷黄优先级最高**  
   刷黄是核心闭环，优先做到：
   
   ```text
   找黄 → 筛选 → 派将 → 出征 → 状态回查 → 战斗结果读取
   ```

---

# 任务进度表

| 阶段 | 模块 | 目标 | 具体任务 | 真机验证方式 | 通过标准 | 优先级 |
|---|---|---|---|---|---|---|
| 0 | 基线准备 | 建立可持续验证环境 | 确认自研辅助 App 可安装、可启动、可查看日志 | 手机打开自研 App，ADB 可看到日志 | App 不闪退，日志可追踪当前账号、区服、页面动作 | P0 |
| 0 | 基线准备 | 建立对照样本 | 梳理“小黄点辅助.apk”的功能菜单、页面、按钮、字段 | 真机打开旧辅助，对每个页面截图 | 形成一份“旧辅助功能清单” | P0 |
| 0 | 基线准备 | 建立测试记录 | 建立测试账号、区服、角色状态记录表 | 使用测试账号登录 351 区 | 能稳定进入角色，并记录初始资源、将领、兵力 | P0 |
| 1 | 登录/区服 | 打通账号入口 | 自研 App 支持账号登录、选择 351 区服、获取基础会话状态 | 手机输入账号，选择区服 | 登录成功，能识别角色 ID/昵称/区服 | P0 |
| 1 | 协议基础 | 建立接口字典 | 把登录、角色、将领、地图、出征等接口整理成文档 | 每次请求后在 App 内展示调试信息 | 请求可复现，响应字段有初步含义 | P0 |
| 1 | 协议基础 | 建立错误处理 | 处理登录失败、区服错误、会话过期、网络失败 | 故意断网/输错密码测试 | App 给出明确错误，不崩溃 | P0 |
| 2 | 角色相关 | 角色资料 | 获取角色基础资料：名称、等级、资源、城池、VIP/状态等 | 打开自研 App 的“角色资料”页 | 显示数据与游戏内一致 | P0 |
| 2 | 角色相关 | 英雄将领信息 | 获取将领列表、等级、体力、状态、所在位置、带兵情况 | 对比游戏内将领页 | 将领数量、名称、状态一致 | P0 |
| 2 | 角色相关 | 军队信息 | 获取当前兵种数量、空闲兵、已出征兵、伤兵等 | 对比游戏内军队/兵营 | 关键数量误差为 0 | P0 |
| 2 | 角色相关 | 宝物信息 | 获取宝物列表、装备状态、属性加成 | 对比游戏内宝物页 | 宝物名称、装备关系一致 | P1 |
| 2 | 角色相关 | 状态信息 | 获取保护、免战、行军、队列、冷却等状态 | 游戏内触发状态后刷新自研 App | 状态变化能及时刷新 | P1 |
| 3 | 军事相关 | 配兵读取 | 读取当前每个将领的配兵情况 | 打开“配兵”页 | 每个将领带兵数量与游戏一致 | P0 |
| 3 | 军事相关 | 配兵修改 | 支持给指定将领分配指定兵种和数量 | 手机上选择将领和兵种执行配兵 | 游戏内实际配兵成功 | P0 |
| 3 | 军事相关 | 配兵保护 | 增加兵力不足、将领忙碌、队列占用检查 | 故意选择不可用将领/兵种 | App 阻止错误操作并提示原因 | P0 |
| 3 | 军事相关 | 副本读取 | 获取副本列表、次数、状态 | 对比游戏内副本页面 | 副本状态一致 | P1 |
| 3 | 军事相关 | 副本操作 | 支持进入/扫荡/挑战可用副本 | 真机执行一次低风险副本 | 游戏内次数变化正确，奖励/状态可回查 | P2 |
| 4 | 刷黄 MVP | 地图山贼搜索 | 根据坐标/范围查找山贼、黄巾、目标 NPC | 在自研 App 输入范围搜索 | 能返回真实地图目标列表 | P0 |
| 4 | 刷黄 MVP | 目标筛选 | 支持按等级、距离、类型、可攻击状态筛选 | 设置筛选条件刷新 | 列表只保留符合条件目标 | P0 |
| 4 | 刷黄 MVP | 将领选择 | 支持选择空闲将领、校验体力/兵力/状态 | 选择一个可用将领 | App 能判断能否出征 | P0 |
| 4 | 刷黄 MVP | 单次刷黄 | 对一个符合条件目标发起出征 | 真机点击“出征/剿灭” | 游戏内出现行军队列，目标正确 | P0 |
| 4 | 刷黄 MVP | 状态回查 | 出征后轮询行军、战斗、返回状态 | 自研 App 查看任务进度 | 能显示出征中/战斗中/返回/完成 | P0 |
| 4 | 刷黄 MVP | 结果读取 | 读取战报或结果，判断胜负、损兵、奖励 | 战斗完成后刷新 | 结果与游戏内战报一致 | P0 |
| 4 | 刷黄 MVP | 连续刷黄 | 支持按规则连续寻找目标并派将 | 设置 3 次连续任务 | 能完成至少 3 次闭环，不错派 | P0 |
| 4 | 刷黄 MVP | 安全阈值 | 增加体力不足、兵力不足、损兵过高、目标过远停止条件 | 人为设置低阈值测试 | 达到阈值后自动停止 | P0 |
| 5 | 军情 | 军情列表 | 获取来袭、出征、返回、驻防、战报等军情 | 游戏内制造一条军情后刷新 | 自研 App 显示一致 | P1 |
| 5 | 军情 | 军情详情 | 查看某条军情详情 | 点击军情详情 | 时间、目标、部队、结果一致 | P1 |
| 5 | 军情 | 军情提醒 | 对来袭、战斗完成做本地提醒 | 触发来袭/返回状态 | 手机通知或 App 内提示正常 | P2 |
| 6 | 打矿 | 矿点搜索 | 搜索指定等级/类型矿点 | 输入范围搜索 | 返回真实矿点列表 | P1 |
| 6 | 打矿 | 矿点筛选 | 按等级、距离、占用状态筛选 | 设置筛选条件 | 列表符合条件 | P1 |
| 6 | 打矿 | 派兵占矿 | 指定将领出征矿点 | 真机执行一次占矿 | 游戏内行军/占矿状态正确 | P1 |
| 6 | 打矿 | 占矿回查 | 读取驻守、收益、返回状态 | 等待状态变化后刷新 | 与游戏内一致 | P1 |
| 7 | 六部 | 六部状态 | 读取六部任务/职位/冷却/可执行状态 | 打开六部页 | 数据与游戏内一致 | P2 |
| 7 | 六部 | 六部操作 | 支持常用六部动作 | 真机执行一次低风险操作 | 操作成功且状态更新 | P2 |
| 8 | 常规/日常 | 常用功能清单 | 对照旧辅助整理常用按钮 | 手机逐页对照 | 自研 App 菜单覆盖主要入口 | P1 |
| 8 | 常规/日常 | 日常任务读取 | 读取每日任务、奖励、次数 | 对比游戏内日常页面 | 状态一致 | P1 |
| 8 | 常规/日常 | 日常执行 | 支持一键完成可自动化日常 | 真机执行部分日常 | 成功执行，失败可提示原因 | P2 |
| 9 | 稳定性 | 日志系统 | 每个请求、响应、错误、操作结果可追踪 | 真机操作后导出日志 | 能定位每次失败原因 | P0 |
| 9 | 稳定性 | 防误操作 | 增加二次确认、黑名单目标、资源/兵力保护 | 设置保护条件后尝试操作 | App 阻止危险操作 | P0 |
| 9 | 稳定性 | 断线恢复 | 会话过期、网络断开后自动恢复或提示重新登录 | 断网/切后台/锁屏测试 | 不崩溃，能恢复或提示 | P1 |
| 9 | 稳定性 | 回归测试 | 每次新构建后跑固定测试清单 | 安装新版 App 后逐项测试 | 关键功能不回退 | P0 |

---

# 推荐里程碑顺序

## M1：只读数据闭环

目标：自研 App 能真实显示账号数据。

验收标准：

- 能登录 351 区；
- 能显示角色资料；
- 能显示将领列表；
- 能显示军队信息；
- 能显示基础状态；
- 数据与游戏内页面一致。

优先完成：

1. 登录/区服；
2. 角色资料；
3. 英雄将领；
4. 军队信息；
5. 状态信息。

---

## M2：配兵闭环

目标：从自研 App 修改游戏内真实配兵。

验收标准：

- 选择将领；
- 选择兵种和数量；
- 点击配兵；
- 游戏内查看配兵已变化；
- 自研 App 刷新后能读到新配兵。

---

## M3：刷黄最小可用版

目标：完成一次真实：

```text
找黄 → 派将 → 出征 → 回查
```

验收标准：

- 搜索到山贼/黄巾目标；
- 按条件筛选；
- 选择空闲将领；
- 发起出征；
- 游戏内出现行军；
- 自研 App 能显示任务状态；
- 战斗结束后能读取结果。

这是当前最核心里程碑。

---

## M4：连续刷黄

目标：让刷黄从单次操作变成可控的连续任务。

验收标准：

- 设置目标等级；
- 设置搜索范围；
- 设置将领池；
- 设置停止条件；
- 连续完成 3 次以上；
- 不错派、不重复打不可用目标；
- 兵力/体力不足时自动停止。

---

## M5：外围功能补齐

目标：逐步补齐旧辅助常用功能。

顺序建议：

1. 军情；
2. 打矿；
3. 日常；
4. 六部；
5. 副本；
6. 宝物；
7. 提醒和日志。

---

# 真机回归测试清单

每次安装新版 App 后，按这个顺序测试：

| 编号 | 测试项 | 是否必须通过 | 测试结果 | 备注 |
|---|---|---|---|---|
| T001 | App 启动不闪退 | 必须 | 未测 |  |
| T002 | 登录测试账号成功 | 必须 | 未测 |  |
| T003 | 选择 351 区成功 | 必须 | 未测 |  |
| T004 | 角色资料正确 | 必须 | 未测 |  |
| T005 | 将领列表正确 | 必须 | 未测 |  |
| T006 | 军队信息正确 | 必须 | 未测 |  |
| T007 | 配兵读取正确 | 必须 | 未测 |  |
| T008 | 单次配兵成功 | 必须 | 未测 |  |
| T009 | 搜索山贼成功 | 必须 | 未测 |  |
| T010 | 筛选山贼成功 | 必须 | 未测 |  |
| T011 | 单次刷黄出征成功 | 必须 | 未测 |  |
| T012 | 刷黄状态回查成功 | 必须 | 未测 |  |
| T013 | 战斗结果读取成功 | 必须 | 未测 |  |
| T014 | 连续刷黄 3 次成功 | 核心 | 未测 |  |
| T015 | 兵力不足时停止 | 核心 | 未测 |  |
| T016 | 体力不足时停止 | 核心 | 未测 |  |
| T017 | 网络异常不崩溃 | 必须 | 未测 |  |
| T018 | 会话过期可恢复/提示 | 必须 | 未测 |  |
| T019 | 日志可导出 | 必须 | 未测 |  |
| T020 | 旧版本关键功能无回退 | 必须 | 未测 |  |

---

# 建议下一步

优先从 **M1：只读数据闭环** 开始，因为这是后面刷黄、打矿、配兵的基础。

建议优先建立或补充以下文档：

```text
/Users/huangchangwei/Desktop/gitSpaceC/Toy/帝王三国/任务.md
/Users/huangchangwei/Desktop/gitSpaceC/Toy/帝王三国/真机测试记录.md
/Users/huangchangwei/Desktop/gitSpaceC/Toy/帝王三国/接口字典.md
```

当前最建议的执行顺序：

1. 梳理旧辅助 App 功能菜单和截图；
2. 检查自研辅助源码当前已有功能；
3. 找出登录、区服、角色资料相关接口；
4. 实现并真机验证 M1；
5. 再进入配兵和刷黄闭环。

---

# 2026-07-08 刷黄“保存设置”闭环修复记录

## 本次修复结论

电脑端 `http://127.0.0.1:17351/index.html` 的“保存设置”已经不再是无反应：

```text
保存设置
→ 后台任务 auto-brush-yellow 启动
→ 041540 寻找山贼
→ 按 5203 = 步≤5、弓≤2、骑≤0、车≤3 筛选
→ 选择命中目标
→ 出征前配兵/补兵
→ 15200a0 prepare
→ 15220a0 expedition
→ 0x8522 status=0 确认出征成功
→ 等待返回
→ 治疗/补兵节点
→ 下一轮或达到上限停止
```

## 关键协议修复

旧链路优先发送：

```text
1520030 + 1522030
```

在当前刷黄山贼场景下会被游戏服返回：

```text
0x8522 = ff0000
```

已改为优先发送实测可用的刷黄 actionType=10：

```text
15200a0 + 15220a0
```

同时 0x8540 目标 ID 对山贼出征新增候选：

```text
raw10_tail8_id_plus_utf_len
```

## 真实验证证据

电脑端真实账号验证成功文件：

```text
/Users/huangchangwei/Desktop/gitSpaceC/Toy/帝王三国/电脑端辅助前端/reports/brush_yellow_1783493024886.json
```

关键字段：

```text
success=true
successVariant=10
successTargetCandidate=raw10_tail8_id_plus_utf_len
```

## 当前测试账号兵力情况

测试账号当前轻骑兵不足 200：

```text
车1：179 轻骑兵
骑1：21 轻骑兵
城内可补库存未达到 200 轻骑兵
```

因此当用户配置“将领A + 200 轻骑兵”但库存不足时，系统现在会明确停止并提示原因，而不是无反应：

```text
出征前兵力未达到配置：需要 200 轻骑兵；已停止，避免无效出征
```

用当前实际可满足的 `车1 + 179 轻骑兵 + 5203` 已真实跑通 1 次刷黄。

## 手机端状态

已同步到手机端源码并安装到真机：

```text
./gradlew testDebugUnitTest --no-daemon   # BUILD SUCCESSFUL
./gradlew assembleDebug --no-daemon       # BUILD SUCCESSFUL
adb install -r app-debug.apk              # Success
```

手机端同步内容：

- 刷黄 payload builder 默认 actionType=10；
- 0x8522 status=0 解析为出征成功；
- 手机“保存设置”会开启 brush-yellow 专用真实动作 gate；
- 保存刷黄后后台链路日志会显示：配兵→041540找黄→5203筛选→出征→治疗→补兵。


## 2026-07-08 追加：等待将领真实返回

进一步补齐“等将领A回来以后再治疗/补兵/下一轮”的语义：电脑端后台不再只按固定秒数 sleep，而是在出征成功后调用 `0x1016 → 0x8004` 刷新将领状态，确认指定将领 `status=0` 或状态未知可继续后，才进入治疗伤兵、补兵和下一轮。

新增日志示例：

```text
等待将领返回基础间隔 N 秒，然后回查 0x8004 状态
回查将领状态：车1 status=0 体力=... 带兵=...轻骑兵
```

如果超过 `maxReturnWaitSec` 仍未返回，会停止任务并提示：

```text
等待将领 <id> 返回超时，已停止，避免重复出征
```


## 2026-07-08 15:12 保存设置→自动刷黄闭环复核

- 电脑端服务：`http://127.0.0.1:17351/index.html`，`/api/health` 正常。
- 复核结论：点击/调用“保存设置”已经不是无反应；会创建 `auto-brush-yellow` 后台任务，并写入日志。
- 账号当前兵力限制：测试账号当前无法满足 `200 轻骑兵`。用任意将领配置 200 时，后台会先找黄并筛选，但在出征前配兵/补兵校验处停止，明确提示“出征前兵力未达到配置”，避免无效出征。
- 使用当前可满足配置 `车1(12886835) + 179 轻骑兵 + 5203 + 1级山贼 + 上限1次` 真实跑通：
  1. 保存设置启动后台；
  2. `041540` 找山贼；
  3. 按 `5203 = 步≤5、弓≤2、骑≤0、车≤3` 筛选；
  4. 选择命中目标；
  5. `15200a0 + 15220a0` 出征，`0x8522 status=0` 成功；
  6. 回查 `0x8004` 确认将领返回；
  7. 执行治疗伤兵与补兵；
  8. 达到上限 1 次后结束。
- 最新证据文件：`/Users/huangchangwei/Desktop/gitSpaceC/Toy/帝王三国/电脑端辅助前端/reports/brush_yellow_1783494692747.json`
  - `success=true`
  - `successVariant=10`
  - `successTargetCandidate=raw10_tail8_id_plus_utf_len`
- 手机端同步检查：`./gradlew testDebugUnitTest assembleDebug --no-daemon` 通过；已重新安装 Debug APK 到连接手机，`adb install -r .../app-debug.apk` 成功。


## 2026-07-08 15:32 手机端保存刷黄复核与修复

- 真机复核发现两个手机端问题：
  1. 旧 session 过期时，点击保存会启动后台但立即 `NeedRelogin(0x1016 未返回 0x8004，实际 0x8016: 没有角色信息)`；已通过重新登录验证，用户需要先保持真实登录态。
  2. 手机端真实刷黄出征响应中，`0x8522` 二进制状态已经是成功，但文本预览可能混入历史战报，旧逻辑先按文本解析，导致误报“响应中出现战报成功文本，但未匹配本次目标坐标/ID”。已改为对刷黄真实二进制 action 优先解析 `responseHex`，`hex-8522-status-0` 作为本次出征成功依据。
- 同步修复手机端循环状态：
  - 将刷黄本地消耗次数和“待将领返回后治疗”的 pending recovery 从 `ShuaHuangTask` 实例内存迁到 `AutomationRuntimeStateStore`，避免服务每 tick 重建 task 后丢失状态。
  - 出征成功后写入 `brush-pending-recovery=<generalId>`；下一 tick 看到将领已空闲后先 `post-return-heal`，再等待下一 tick 让配兵/补兵任务先执行，避免“治疗后未补兵就再次出征”。
  - 登录区服输入框默认填入 `周年服351区(新服)`，避免用户不填区服时误选第一个区。
- 真机验证日志片段（已 force-stop 停止测试后台，避免持续刷）：
  - `保存刷黄：将领/编队=12886835，筛选=5203...后台启动=true`
  - `brush-yellow search-target start=0,0 type=SHAN_ZEI`
  - `targets total=8 matched=8`
  - `真实刷黄 expedition 返回：p2=10 opcode=15220a0 ... success=true msg=出征成功`
  - `brush-yellow dispatch-success formation=12886835 ... consumed=1`
  - `brush-pending-recovery=12886835`
  - `post-return-heal general=12886835 success=true message=治疗成功`
- 构建验证：`./gradlew testDebugUnitTest assembleDebug --no-daemon` 通过；已重新 `adb install -r app/build/outputs/apk/debug/app-debug.apk` 安装到手机。


## 2026-07-08 17:05 电脑端实时数据替换死数据修复

- 问题：电脑端登录切换测试账号后，角色/英雄/军队/宝物/状态页仍显示原型死数据；底部黑色日志区默认显示旧假日志。
- 修复：
  - 后端 `/api/login` 追加返回 `roleState`：从真实 `0x1016/0x8004` 解析君主、等级、铜钱、粮食、声望、人口、资源点、封地上限、将领上限等。
  - 后端追加尝试 `0x1104/0x8104` 背包解析，返回 `inventory.items`，避免宝物页继续展示原型物品。
  - 前端 `renderRole()` 改为完全绑定 `appState.roleState / appState.generals / appState.inventory`；未解析到的数据展示“暂无实时数据”，不再用小黄点原型死数据兜底。
  - 顶部账号卡改为显示“实时 / 接口状态 / 已同步 / 账号@区服 · 君主 · Lv”。
  - 黑色日志区删除旧 `[04:00:47]` 假日志；切换账号重新登录时清空旧日志，只显示当前账号登录、资源、保存设置、后台任务轮询日志，并自动滚动到底部。
- 验证：浏览器登录测试账号 `1608601` / `周年服351区` 后：
  - 角色页显示：董全 Lv.12，铜钱/粮食/声望/人口/资源点等实时数据。
  - 英雄页显示：勇民2、步1、车1、骑1、少·刘协之等真实将领及实时体力/带兵。
  - 日志区显示当前登录日志与实时资源摘要。
- 验证截图：
  - `/Users/huangchangwei/Desktop/gitSpaceC/Toy/帝王三国/电脑端辅助前端/preview_live_role.png`
  - `/Users/huangchangwei/Desktop/gitSpaceC/Toy/帝王三国/电脑端辅助前端/preview_live_generals.png`


## 2026-07-08 追加：模仿小黄点的后台保活/掉线检测能力

### 手机端自研辅助 App 必做能力

参考 `reverse_case_小黄点辅助` 的 Manifest 与布局证据，小黄点辅助具备后台挂机服务、VPN/本地代理、掉线重连配置、后台保护提示等能力。自研 App 后续需要补齐：

1. **后台 Service**
   - 用于承载刷黄、保活、掉线检测、状态轮询等长期任务。
   - 前台页面退出或息屏后仍能运行。

2. **VPN / 本地代理服务**
   - 用于后续抓包、协议转发、账号接口请求代理和本地调试。
   - 参考小黄点的 `LocalService`、`MainService`、`FirewallVpnService` 结构。

3. **防息屏 / 后台清理相关能力**
   - 申请/使用前台服务、WakeLock、网络状态监听。
   - UI 提示用户把 App 加入省电管家/手机管家保护名单，避免锁屏后被清理。

4. **网络状态检测**
   - 监听网络断开/切换。
   - 区分网络异常、session 失效、被顶号/掉线。

5. **掉线检测与重连策略**
   - 默认可参考小黄点：掉线 10 秒后重连。
   - 支持策略：掉线后自动重新登录 / 掉线后停止后台任务。

### 电脑端已实现方向

电脑端不做 VPN/手机后台常驻，只做本地后端保活：

- 新增 `/api/heartbeat`：优先发送 `0x3110 payload=0100`，期望 `0xa110`，用于账号在线检测。
- 新增账号状态：`未开启 / 检测中 / 开启 / 掉线`。
- 添加账号后仅加入账号下拉框，不自动启动；点击“启动”后才开启该账号心跳保活。
- 下拉框切换账号只切换前端展示数据，不改变该账号后台状态。
- 删除按钮删除当前所选账号及其内存 session/配置/任务。
