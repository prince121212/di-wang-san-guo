# 交接文档 7-9

## 项目目标

本项目目标是做一个《帝王三国》的自研辅助 App / 服务端工具链，通过抓包和逆向协议解析游戏账号真实接口数据，并在自研辅助 App 或电脑端辅助里展示、管理、调用接口，实现自动操作。

当前优先级最高的是刷黄链路：账号登录 -> 同步角色/将领/兵力状态 -> 配兵规则检查或补兵 -> 地图找山贼 -> 按规则筛选 -> 指派指定将领出征 -> 等待返回 -> 查看战报/军情 -> 循环。

## 已知资产

- 旧辅助 APK：`/Users/huangchangwei/Desktop/gitSpaceC/Toy/帝王三国/小黄点辅助.apk`
- 旧辅助逆向材料：`/Users/huangchangwei/Desktop/gitSpaceC/Toy/帝王三国/reverse_case_小黄点辅助`
- 自研辅助源码：`/Users/huangchangwei/Desktop/gitSpaceC/Toy/帝王三国/自研辅助源码`
- 游戏本体 APK：`/Users/huangchangwei/Desktop/gitSpaceC/Toy/帝王三国/三国·帝王联盟1.66.apk`
- 电脑端辅助前端/后端：`/Users/huangchangwei/Desktop/gitSpaceC/Toy/帝王三国/电脑端辅助前端`
- 设计稿：`/Users/huangchangwei/Desktop/gitSpaceC/Toy/帝王三国/帝三辅助设计`
- 抓包输出目录：`/Users/huangchangwei/Desktop/gitSpaceC/Toy/帝王三国/ctf_out`

## 测试账号

- 账号 1：`1608601`
- 账号 2：`1608602`
- 密码：`123459`
- 区服：`351`，通常显示为 `周年服351区(新服)`

注意：添加账号只是在本地添加账号记录，不应真实登录。只有点击“启动”才执行真实登录并开启保活。

## 当前电脑端辅助运行方式

电脑端辅助地址：

```text
http://127.0.0.1:17351/index.html
```

后端必须运行：

```bash
cd /Users/huangchangwei/Desktop/gitSpaceC/Toy/帝王三国/电脑端辅助前端
python3 -u server.py
```

不要再使用：

```bash
python3 -m http.server 17351
```

原因：现在 `server.py` 不只是静态文件服务，还负责账号、登录、协议请求、保活、状态持久化和自动刷黄。

当前常用 screen：

- 后端服务：`dwsanguo_helper`
- 抓包：`diwang_live_pcap`
- 抓包时间线记录：`diwang_timeline_logger`

检查命令：

```bash
lsof -nP -iTCP:17351 -sTCP:LISTEN
screen -ls
```

最近一次已重启后端到新代码，监听端口为 `127.0.0.1:17351`。

## 重要持久化文件

电脑端辅助运行态主要落在：

```text
/Users/huangchangwei/Desktop/gitSpaceC/Toy/帝王三国/电脑端辅助前端/reports/runtime_state.json
```

重启 `server.py` 前后应尽量保留这个文件。账号记录、部分运行状态、规则配置等依赖它恢复。

## 当前电脑端辅助已实现/调整

### 账号逻辑

- 添加账号：只添加本地记录，不真实登录。
- 启动账号：才真实登录游戏服，并开启后台保活。
- 关闭账号：关闭本地会话，账号状态变为未开启/停止。
- 删除账号：删除当前下拉框选中的账号记录。
- 切换账号下拉框：只切换前端展示的数据，不改变账号启动状态。
- 右上状态：
  - 在线显示绿色 `开启`
  - 掉线显示灰色 `掉线`
  - 未启动显示 `未开启`

### 多容器展示

- 前端支持右上角 `+容器`。
- 容器只是展示框，不影响后台任务。
- 当前一个容器也能切换/展示多个账号。
- 多容器目前主要是视觉展示，不是独立任务沙箱。

### 日志页

- 日志已经独立为底部页面，和“助手”同级。
- 右上角有“助手 / 日志”快捷按钮。
- 助手页不再展示底部黑色日志框。

### 保存提示

- 各页面保存时会在顶部弹出约 1 秒提示。
- 配兵保存提示：`保存配兵设置成功`
- 刷黄保存提示：保存刷黄规则并启动后台。

## 最近一次关键改动

### 角色子页面刷新逻辑

文件：

```text
/Users/huangchangwei/Desktop/gitSpaceC/Toy/帝王三国/电脑端辅助前端/app.js
/Users/huangchangwei/Desktop/gitSpaceC/Toy/帝王三国/电脑端辅助前端/server.py
```

已删除「角色」侧栏最下面的“立即刷新”按钮。

现在点击角色子页面按钮时会自动刷新对应数据：

- 点击「角色」：刷新 `0x1016 -> 0x8004`
- 点击「英雄」：刷新 `0x1016 -> 0x8004`
- 点击「军队」：刷新 `0x1016 -> 0x8004`
- 点击「状态」：刷新 `0x1016 -> 0x8004`
- 点击「宝物」：刷新 `0x1104 -> 0x8104`

前端通过 `/api/state/refresh?sessionId=...&scope=...` 控制刷新范围。

scope 当前约定：

- `role`：角色/英雄/军队/状态相关，走 `0x1016 -> 0x8004`
- `inventory`：宝物/背包，走 `0x1104 -> 0x8104`
- `military`：军情，走 `0x3110 -> 0xa110`
- `all`：全部刷新

### 保活节奏

已根据抓包调整后台保活节奏。

抓包观察到游戏客户端基本每约 20 秒发一次：

```text
0x3110 payload=0100 -> 0xa110
```

偶尔会和：

```text
0x6200 -> 0xe200
```

合包。

当前电脑端后台保活线程已改为约 20 秒一次 `0x3110 -> 0xa110`，常量在：

```text
server.py: CLIENT_HEARTBEAT_INTERVAL_SEC = 20
```

已移除之前前端每 8 秒自动刷新状态的轮询，避免节奏不像客户端。

### 军情自动更新

`0x3110 -> 0xa110` 不只是心跳，也包含混合实时信息：

- 将领忙闲状态记录
- 出征/返回/胜利文本
- 战斗结果提示
- 部分活动/任务信息

现在每次后台调用心跳 `0x3110 -> 0xa110` 时，会自动解析并更新：

```text
sess["militaryIntel"]
```

也就是说：只要账号已启动，心跳保活返回后，军情页的数据会通过账号轮询同步到前端。

## 已知协议/接口

### 登录与角色

- `0x1003 -> 0x8003`
  - 游戏服登录。
  - 返回角色列表、dm 等。
- `0x1004`
  - 进入/选择角色前后相关，目前登录流程里会发送。
- `0x1016 -> 0x8004`
  - 角色/君主/资源/将领/军队信息核心接口。
  - 当前用于角色页、英雄页、军队页、状态页。
  - 将领状态的很多基础字段从这里解析。

### 宝物/背包

- `0x1104 -> 0x8104`
  - 当前用于宝物页。
  - 道具名映射文件：

```text
/Users/huangchangwei/Desktop/gitSpaceC/Toy/帝王三国/reverse_cases/apk-sanguo-diwanglianmeng-166/analysis/game_rules/item_mapping/item_full_mapping.json
```

当前已知道具映射约 `758` 个，ID 大概 `0-757`。

之前样例解析出：

- 屯田令 5
- 鲁公手册 1
- 兵书 1
- 新手礼包2 1
- 山贼头巾 16

注意：`0x8104` 解析目前仍偏保守，复杂背包记录可能还会漏。

### 心跳/实时状态/军情混合包

- `0x3110 payload=0100 -> 0xa110`

用途：

- 后台保活/掉线检测
- 将领忙闲状态
- 最近出征/返回/胜利文本
- 军情页临时数据源

当前判断：它不是干净的“军情列表接口”，而是实时混合包。

将领状态目前按 `0xa110` 将领记录解析：

- 有任务/行军字段：`征`
- 无任务/行军字段：`闲`

之前用户提过“回”，后来抓包和客户端表现看起来并不会直接给“回”字，将返回只作为军情文本，不作为英雄页状态。

### 军情/战报详情疑似接口

- `0x1702 -> 0x8702`

当前判断：疑似“某条军情/战报详情查询”，不是军情列表入口。

抓包证据：

- 出征返回 `0x8522` 中出现事件/战报 ID，例如 `0x59a85b`
- 后续客户端发 `0x1702` 带 ID
- 服务端返回 `0x8702`，里面有将领名、战斗相关结构

还没有完全确认：

- 军情列表从哪个接口来
- `0x1702` 请求体第一个字段 type 的完整含义
- `0x8702` 结构完整字段

### 出征/刷黄相关

- `0x1522 -> 0x8522`
  - 出征/剿灭山贼相关。
  - `0x8522` 响应里会出现战斗文本、目标信息、将领快照、事件 ID。

抓包样例：

```text
【消灭】骑牛者消灭1级山贼(71,8)
```

### 活动/任务包

- `0x6200 -> 0xe200`
  - 活动/任务进度混合文本。
  - 不应直接混到军情页里展示。

## 抓包结论

最近重点抓包目录：

```text
/Users/huangchangwei/Desktop/gitSpaceC/Toy/帝王三国/ctf_out/passive_pcap_hotspot_20260709_045051
/Users/huangchangwei/Desktop/gitSpaceC/Toy/帝王三国/ctf_out/passive_pcap_hotspot_20260709_042546
```

`20260709_045051` 中 `0x3110` 心跳时间戳反推结果：

- 首次后约 23 秒
- 稳定后约 20.1-20.4 秒一次
- 因此电脑端保活节奏目前设为 20 秒。

有用 flow：

- `034`：`0x1522 -> 0x8522`，出征返回，包含 `【消灭】骑牛者消灭1级山贼(71,8)`
- `043`：`0x1702 -> 0x8702`，疑似详情查询，响应包含 `骑牛者`
- `044-048`：连续 `0x1702 -> 0x8702`，可能是详情/分页/追踪查询
- `050`：`0x3110 + 0x6200 -> 0xa110 + 0xe200`，包含胜利、返回、升级、活动文本
- `057`：`0x3110 -> 0xa110`，只含将领状态类记录

## 将领状态现状

当前英雄页状态应展示：

- `闲`
- `征`
- `未知`

注意：

- 不要从“返回”文本推断英雄页状态为 `回`。
- `返回` 只作为军情事件文本展示。
- 将领忙闲应优先来自 `0xa110` 的将领记录。
- 如果 `0xa110` 无法确认，则不要假装空闲，应显示未知或阻止自动出征。

之前已修复一个解析误判：

- `少·刘协之` 不是测试账号将领，不应展示到英雄栏。
- 这类是名将卡/说明文字误命中，解析时要避免 name-only 命中。

## 刷黄状态机要求

任何自动出征前必须按状态机检查，不满足就停止并写清楚日志。

必须检查：

- 账号是否已启动且在线
- 会话是否有效
- 今日刷黄次数是否到上限
- 指定将领是否存在
- 指定将领 `0xa110` 状态是否为 `闲`
- 指定将领体力是否足够，通常要求大于 20
- 指定将领当前配兵是否符合「军事 - 配兵」页保存规则
- 如开启批量补满，则出征前可请求补兵
- 配兵/补兵后必须重新刷新并确认状态

任何状态不满足，不要继续找黄或出征，只在日志中说明具体原因。

## 配兵/刷黄前端规则

### 军事 - 配兵

当前设计：

- 删除了“复制编队”
- “添加编队”点击后添加空编队，用户手动下拉选择将领/兵种/数量
- “一键删除”清空当前全部编队信息
- 保存设置只保存配兵规则，不启动刷黄

### 刷黄

当前设计：

- 出征将领下拉只展示名字
- 山贼等级下拉
- 掉落允许多选
- 掉落真实候选：`宝物`、`资源`、`装备`、`宝箱`
- 删除了“复制编队”
- 添加编队添加空规则
- 步/弓/骑/车为 4 个小下拉框，可选 `0-5`
- 原“模式：步弓骑车”已改为“批量补满”复选框

注意：刷黄保存设置只保存刷黄规则并启动刷黄任务，不应修改配兵规则。

## 手机端现状

手机端自研辅助 App 已安装在连接手机上，可随时实测。

但目前策略是：

1. 先在电脑端把一个真实链路跑通。
2. 再把已跑通链路逐个移植到手机 App。

已知手机端与电脑端差距：

- 「军事 - 配兵」页电脑端可正常勾选、下拉选择；手机端之前仍有细节未完全同步。
- 刷黄页面控件也需要同步电脑端最新交互。
- 手机端还需要模仿小黄点辅助具备：
  - 后台 Service
  - VPN/本地代理服务
  - 防息屏/后台清理相关能力
  - 网络状态检测

## IP/VPN/代理

之前发现登录失败有可能是 IP 问题。

目标设计：

- 电脑版辅助后台在某个 IP 连不通游戏时，自动切换当前可用 IP。
- 前提：判断失败原因确实像 IP 问题。
- 若有可用 IP，则自动切换。
- 若无可用 IP，则提示：`暂无可用IP，请开启VPN`
- 未来支持多个账号自动分配不同 IP。

相关实现已做过一部分，但下个线程应继续检查实际效果。

## 抓包工具

已有一个简化可视化抓包工具：

```text
/Users/huangchangwei/Desktop/gitSpaceC/Toy/帝王三国/自研辅助源码/tools/passive_hotspot_capture_app.py
```

设计目标：

- 点击即可开启抓包。
- 可选是否只抓某个地址，例如游戏服地址。
- UI 只展示基础抓包记录，让人知道当前抓到了哪个地址的包。
- 原始 TCP 包/hex 不直接展示给用户，但完整保存，方便 AI 分析。

注意：

- 启动 tcpdump 可能需要 sudo 权限。
- 之前遇到过 `sudo: a password is required`。
- 需要通过合理方式处理权限，不能让用户点开始就直接失败。

## 当前页面/服务验证

最近一次验证：

```bash
python3 -m py_compile server.py
node --check app.js
curl -I http://127.0.0.1:17351/index.html
curl http://127.0.0.1:17351/api/accounts
```

结果：

- Python 语法通过
- JS 语法通过
- 页面 200
- 账号接口返回 JSON 正常
- 当前账号记录存在，但状态为 stopped/未开启

## 重要文件索引

电脑端主要文件：

```text
/Users/huangchangwei/Desktop/gitSpaceC/Toy/帝王三国/电脑端辅助前端/index.html
/Users/huangchangwei/Desktop/gitSpaceC/Toy/帝王三国/电脑端辅助前端/app.js
/Users/huangchangwei/Desktop/gitSpaceC/Toy/帝王三国/电脑端辅助前端/styles.css
/Users/huangchangwei/Desktop/gitSpaceC/Toy/帝王三国/电脑端辅助前端/server.py
```

关键函数：

```text
server.py
- fresh_login
- refresh_generals
- refresh_inventory
- parse_8004_head
- recover_generals_from_8004
- parse_8104_inventory
- execute_heartbeat
- heartbeat_worker
- parse_a110_general_statuses
- parse_military_intel_from_a110
- update_military_intel_from_packets
- refresh_military_intel
- start_auto_brush
- auto_brush_worker

app.js
- renderTabs
- renderSide
- renderRole
- renderJunqing
- refreshRoleSide
- refreshLiveState
- syncAccounts
- startSelectedAccount
- stopSelectedAccount
- saveFormationSettings
- saveSettingsAndStart
```

## 下个线程建议优先级

### P0：真实验证启动和保活

1. 打开 `http://127.0.0.1:17351/index.html`
2. 选择账号 `1608601` 或添加 `1608602`
3. 点击“启动”
4. 确认右上状态从未开启变为绿色开启
5. 等待 40-60 秒，确认后台 `0x3110 -> 0xa110` 保活没有掉线
6. 进入「军情」页，看心跳是否自动更新将领状态/事件

### P0：验证角色子页切换刷新

账号启动后依次点击：

- 角色
- 英雄
- 军队
- 宝物
- 状态

预期：

- 切换页面即自动调用接口刷新，不需要额外“立即刷新”按钮。
- 英雄/军队/角色/状态来自 `0x8004`。
- 宝物来自 `0x8104`。

### P0：继续修军情页混乱

当前军情页仍使用 `0xa110` 混合包筛选文本，所以容易乱。

短期改法：

- 军情页分区：
  - 将领状态
  - 最近出征/返回/胜利事件
  - 战报详情候选
- 过滤 `0xe200` 活动/任务文本。
- 对 `0xa110` 事件做去重。
- 不要把活动任务、礼包、资源奖励列表混进军情。

中期改法：

- 深挖 `0x1702 -> 0x8702`。
- 找到军情列表来源后，再改为真正列表 + 详情。

### P0：真实刷黄链路

用测试账号验证：

1. 配兵页保存规则，例如将领 `车1/骑牛者` 分配轻骑兵某数量。
2. 刷黄页选择该将领。
3. 山贼等级选择 1 级。
4. 根据需要勾选/取消“批量补满”。
5. 保存刷黄设置启动后台。
6. 看日志是否严格按状态机走。

不允许出现：

- 账号未登录就找黄
- 指定将领不是闲还出征
- 刷黄页选 A 将领，日志/实际却派 B 将领
- 配兵不符合规则还继续出征

### P1：配兵真实接口

当前仍需继续确认“给某将领配多少某兵种”的真实接口。

用户曾问是否需要手动操作游戏抓包，建议如果现有抓包不足，可以让用户在游戏客户端操作一次：

1. 进入配兵
2. 给某个将领配指定兵种和数量
3. 保存/确认
4. 抓包对比请求

重点找：

- 请求 opcode
- 将领 ID
- 兵种 ID
- 数量字段
- 成功/失败响应

### P1：手机端同步

电脑端真实链路跑通后，把以下功能迁移到手机 App：

- 账号启动/关闭/状态
- 角色/英雄/军队/宝物/状态真实刷新
- 配兵规则页面控件与保存
- 刷黄规则页面控件与保存
- 后台 Service 保活
- VPN/本地代理服务
- 网络状态检测
- 防息屏/后台清理

## 注意事项

- 不要把死数据当实时数据展示。
- 不要在账号未启动时调用游戏动作接口。
- 不要让“添加账号”触发真实登录。
- 不要使用 UI 假日志掩盖真实动作。
- 所有自动动作都必须以状态机为准。
- 刷黄日志必须明确说明每一步为什么继续或为什么停止。
- 如果后端重启，尽量不破坏本地账号记录和配置。
- 若账号掉线，要及时更新账号状态。
- 如果请求失败疑似 IP 问题，优先走可用 IP/VPN 切换逻辑。

## 交接给下个线程的一句话

当前最稳的方向是：电脑端先把 `启动账号 -> 20 秒 0xa110 保活 -> 角色子页按需刷新 -> 状态机刷黄` 跑通；真正军情列表接口还没确认，`0x1702 -> 0x8702` 是下一步重点。
