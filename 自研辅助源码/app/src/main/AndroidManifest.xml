<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <!--
      Package visibility (targetSdk 30+) hides these vendor system apps, which
      makes both resolveActivity() and an explicit-component startActivity()
      behave as if the autostart/battery settings page were not installed.
      Declaring the exact packages we deep-link to is the narrow, policy-safe
      alternative to QUERY_ALL_PACKAGES; the text guidance in
      VendorBackgroundGuidance remains the fallback when a ROM moves the page.
    -->
    <queries>
        <package android:name="com.eg.android.AlipayGphone" />
        <package android:name="com.eg.android.AlipayGphoneRC" />
        <package android:name="com.miui.securitycenter" />
        <package android:name="com.miui.powerkeeper" />
        <package android:name="com.huawei.systemmanager" />
        <package android:name="com.hihonor.systemmanager" />
        <package android:name="com.coloros.safecenter" />
        <package android:name="com.oppo.safe" />
        <package android:name="com.oplus.safecenter" />
        <package android:name="com.vivo.permissionmanager" />
        <package android:name="com.iqoo.secure" />
        <package android:name="com.meizu.safe" />
        <package android:name="com.samsung.android.lool" />
    </queries>
    <application
        android:name=".SharedCoreApplication"
        android:allowBackup="false"
        android:usesCleartextTraffic="true"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.hosting.HostingPermissionGuideActivity" android:exported="false" />
        <activity android:name=".MembershipStoreActivity" android:exported="false" />
        <activity
            android:name=".AlipayPaymentActivity"
            android:exported="false"
            android:configChanges="orientation|screenSize" />
        <!-- Alipay AAR supplies H5PayActivity and its full configChanges list. -->
        <service
            android:name=".service.AssistantForegroundService"
            android:exported="false"
            android:foregroundServiceType="specialUse"
            android:stopWithTask="false">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="User-enabled on-device game assistant scheduling while the phone is locked" />
        </service>
        <receiver
            android:name=".service.BootCompletedReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.USER_UNLOCKED" />
            </intent-filter>
        </receiver>
        <activity
            android:name=".AssistantWebActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
