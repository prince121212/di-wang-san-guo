# 掠夺抓包分析（2026-07-10 06:33）

操作：派 `步1`、`骑1` 两个将领掠夺 `天雄星` 的 `永原封地`，该封地在封地列表中为第 10 条。

结论：本次抓包信息足够开发电脑端“军事-掠夺”的第一版真实功能。

## 关键请求序列

1. `0x1300` 按封地名查询目标：`永原` → 返回 targetId `0x0959`、`永原封地`、君主 `天雄星`。
2. `0x1520` 预出征：`actionType=0x01`，将领 `[0xc4a332, 0xc4a331]`，目标 `0x0959`。
3. `0x1522` 正式出征：抓包样本为“精确到达”，同上，额外 `relatedLong=1`，尾部 flags=`03 19 10`；电脑端默认已改为普通立即出征：`relatedLong=-1`，尾部 flags=`00 00 00`。
4. `0x8522` 响应包含成功文本：`【夺取】步1,骑1夺取天雄星＜学士＞的封地(永原封地)`。

## 精确字段

### 预出征 0x1520 / flow 008

```hex
01020000000000c4a3320000000000c4a3310000000000000959
```

```text
actionType = 0x01
generalCount = 0x02
generalIds = [0x0000000000c4a332, 0x0000000000c4a331]
targetId = 0x0000000000000959
```

### 正式出征 0x1522 / flow 011

```hex
01020000000000c4a3320000000000c4a33100000000000009590000000000000001031910
```

```text
actionType = 0x01
generalCount = 0x02
generalIds = [0x0000000000c4a332, 0x0000000000c4a331]
targetId = 0x0000000000000959
relatedLong = 0x0000000000000001
flags = 03 19 10
```

## 玩家名 + 封地序号解析

### 查询君主概要 0x1120 / flow 031

```hex
010009e5a4a9e99b84e6989f
```

即：`01 + UTF("天雄星")`。

### 查询封地列表 0x1310 / flow 033

```hex
00010009e5a4a9e99b84e6989f
```

即：`0001 + UTF("天雄星")`。

响应 `0x8310` 解析到：

| 序号 | targetId | 名称 | 城/地名 |
|---:|---:|---|---|
| 1 | 0x00b6 / 182 | 天雄星基地 | 临淄 |
| 2 | 0x0125 / 293 | 福关封地 | 福关 |
| 3 | 0x01d5 / 469 | 红包拿来 | 永远 |
| 4 | 0x01d8 / 472 | 福山封地 | 福昌 |
| 5 | 0x0491 / 1169 | 福山封地 | 临淄 |
| 6 | 0x0492 / 1170 | 福山封地 | 临淄 |
| 7 | 0x0955 / 2389 | 余池封地 | 余池 |
| 8 | 0x0956 / 2390 | 永溪封地 | 永溪 |
| 9 | 0x0957 / 2391 | 余海封地 | 余海 |
| 10 | 0x0959 / 2393 | 永原封地 | 永原 |

第 10 条 targetId `0x0959` 与出征请求目标完全一致。

## 开发判断

足够开发：

- 目标解析：`0x1310` 可按玩家名获取封地列表，按 1-based 序号取 targetId。
- 出征构造：`0x1520/0x1522` 字段完整。
- 成功判断：`0x8522` 有明确成功文本。

修正：`0x1522` 尾部 `031910` 实际代表精确到达 `03:25:16`，电脑端默认不能固定使用；普通立即出征应使用 `ffffffffffffffff000000`。
