# 任务分配机制优化规划

> 文档状态：阶段 B 已落地，已改动共享调度生产代码；阶段 C/D/E 仍按原计划推进
>
> 目标：只优化任务分配、轮转和线程归属；刷黄、打矿、副本、无损、掠夺等已有功能的协议、配兵、治疗、出征、恢复和结果判定保持不变。
>
> 本规划暂不处理：抢城、押镖、寻宝。

## 1. 本次要解决的问题

当前各功能已经有自己的完整流程，但任务选择仍容易被理解成“所有任务放在一条固定列表里轮询”。改造后需要做到：

1. 每日任务和循环任务共用一条账号主执行线。
2. 账号登录后，先检查并推进当日任务，再进入循环任务。
3. 同一名将同时被多个循环任务使用时，只允许一个任务占用它。
4. 任务是否参与竞争，取决于它此刻是否真正满足条件，而不是仅仅因为开关已开启。
5. 闲时任务有独立执行线，但不能与主执行线对同一账号并发发游戏请求。
6. 现有功能内部状态机不重写，只替换“下一项任务如何被选中”。

## 2. 任务分类

### 2.1 循环任务：主执行线

这些任务会反复执行，并且可能占用将领、兵力、出征队列或战斗状态：

- 打矿
- 刷黄
- 无损
- 掠夺
- 副本

每个循环任务继续使用当前已有的业务流程和账本。以下动作不是独立顶层任务，而是由当前循环任务按需调用：

- 读取最新角色、资源和将领状态
- 选择将领、目标和规则行
- 粮食转铜
- 治疗伤兵
- 增加忠诚
- 增加体力
- 配兵、补兵、清理其他将领兵力
- 出征预检、预出征、正式出征
- 等待战斗、读取军情、确认将领回闲
- 任务专属恢复：撤防、结算、开副本宝箱、刷黄计数等

两种“开箱”要区分：

- 副本结算后打开所选宝箱：副本循环任务的收尾步骤。
- 主号背包自动开箱：闲时背包任务。

### 2.2 每日任务：主执行线

每日任务按当前账号设置逐项检查，每项拥有独立的当日完成锁：

- 自动签到
- 领取竞技币
- 自动捐献
- 领取国家俸禄
- 国家征收
- 城主征收
- 名将拜访

触发点：

- 账号登录并完成状态初始化后检查一次。
- 到达每日周期边界后检查一次。
- 竞技币沿用现有中国时区 22:00 周期；其他日常沿用 00:00 周期。

“完成每日任务”不是要求所有网络请求永远成功，而是要求每个当日任务都得到本轮确定结论：已完成、服务器已领取、规则不适用、明确失败或需要人工关注。可重试的网络失败保存 `retryAt`，到时间后仍由每日线优先处理，不能因单项失败造成无限空转。

### 2.3 闲时任务：独立执行线

闲时任务只在主执行线没有可立即推进的每日/循环动作时运行：

- 闲时找黄：扫描、筛选并更新山贼目标缓存。
- 闲时找矿：扫描、筛选并更新资源点目标缓存。
- 六部种植：金银花种植；以后确认的收菜、偷菜、礼部动作仍归此组。
- 自动内政：升级房屋、建筑队列维护。
- 自动科技：科技队列维护。
- 主号物品丢弃：按规则清理物品或装备。
- 主号自动开箱：按白名单和钥匙条件开箱。

闲时任务可以被循环任务随时打断，但不能在一个已经越过发送边界的网络动作中途强行取消。

### 2.4 系统观察能力：不作为普通任务排队

以下能力不进入每日/循环/闲时任务列表：

- 登录、重登、Session 恢复、心跳保活。
- 角色和将领状态刷新。
- 军情读取和军情警报。
- 出征不确定账本恢复。
- 日志、成功记录和状态投影。

它们是执行安全和观察层。特别是账本恢复必须先于任何新业务动作，军情警报应是被动观察器，而不是等待“轮到它”才执行的普通任务。

## 3. 两条执行线

### 3.1 主执行线：每日 + 循环

每个账号只有一个主执行线，步骤固定为：

```text
启动/登录
  → 安全恢复未完成账本
  → 每日任务门禁
  → 选择可运行的循环任务
  → 执行一个已有业务步骤
  → 持久化状态和 nextWakeAt
  → 重新计算每日与循环候选
```

每日门禁打开时，不选择循环任务；先按配置顺序检查全部每日任务。每日任务完成、跳过或进入明确终态后，门禁才允许循环任务参与竞争。

### 3.2 闲时执行线

闲时线单独使用一个执行队列/线程，但遵守同一账号的 `GameRequestLock`：

```text
主线没有可运行的每日/循环任务
  → 发放 idle permit
  → 闲时线选择一个到期闲时任务
  → 执行一个可验证步骤
  → 保存状态
  → 主线一旦出现可运行候选，撤销 permit
```

“独立线程”表示调度和等待不阻塞主线，不表示同一账号可以同时发两组游戏协议。所有游戏网络请求仍按账号串行；本地设置、日志和页面读取不受这两条线阻塞。

## 4. 循环任务的选择规则

### 4.1 每次 tick 只做候选评估，不直接按固定列表执行

每次主线唤醒先为每个循环任务生成候选事实：

```text
feature
ruleId
generalIds
eligible
blockedReason
nextEligibleAtMillis
priority
```

只有 `eligible=true` 的任务才进入竞争。未满足条件的任务必须返回具体原因和下一次检查时间，例如：没有目标、将领忙碌、达到每日上限、冷却中、资源不足或等待战斗结束。

### 4.2 同一名将的互斥

将领锁按实际将领 ID 保存，而不是按任务类型保存：

- 同一名将被打矿占用时，刷黄和副本只能显示“等待将领”，不能执行前置配兵或发包。
- 高优先级任务没有目标、达到上限或处于冷却时，应释放竞争资格，低优先级任务才能被选中。
- 高优先级任务只是暂时等待服务器返回时，只锁定它实际占用的将领；其他不冲突的将领是否可被使用，按现有功能账本能力逐步放开，第一阶段不改功能内部并发语义。

### 4.3 已确认的优先级关系

本次先固定用户明确的关系：

```text
打矿 > 刷黄 > 副本
```

因此，当同一名将同时满足三个任务条件时：

1. 先执行打矿。
2. 打矿没有可执行目标、达到限制、进入冷却或不再满足条件后，重新评估刷黄。
3. 刷黄同样不可执行后，再评估副本。

无损和掠夺的相对位置先保留当前共享契约的既有顺序，不在本次规划中擅自改变；后续只需调整优先级表，不触碰它们的业务实现。

### 4.4 不做无意义重试

任务返回 `WAITING` 时，不应立即再次调用同一任务：

- 写入 `nextEligibleAtMillis`。
- 主线重新评估其他候选。
- 所有候选都不可运行时，才等待最早 deadline。

这样可以避免一个没有目标或将领忙碌的任务霸占主线。

## 5. 不改变已有功能实现的改造边界

本次只允许修改：

- 候选任务分类和任务元数据。
- 每日门禁状态。
- 循环任务候选评估、将领锁和优先级选择。
- 闲时线程的唤醒、让步和取消许可。
- `nextWakeAtMillis` 的合并规则。
- 任务栈的状态投影和测试。

本次禁止修改：

- 刷黄、打矿、副本、无损、掠夺内部协议和 payload。
- 现有配兵、治疗、加体、加忠、粮转铜规则。
- 出征账本发送边界和 `UNCERTAIN` 恢复语义。
- 每日功能的协议、完成锁和服务器成功判定。
- 电脑端与 Android 的共享 Python 所有权边界。

## 6. 分阶段实施顺序

### 阶段 A：只增加分类，不改变行为

- 给每个设置功能增加 `lane`：`daily`、`cycle`、`idle`、`observation`。
- 将配兵、治疗、加忠、加体、粮转铜标记为 `substep`，不生成顶层任务。
- 抢城、押镖、寻宝暂不加入本阶段。
- 用旧调度结果回放，确认分类本身不改变现有功能。

### 阶段 B：建立每日门禁

- 登录和每日边界创建 `dailyEpoch`。
- 主线先逐项执行七类日常，再开放循环候选。
- 每项日常独立记录 `DONE/SKIPPED/FAILED/UNCERTAIN/RETRY_AT`。
- 增加“单项失败不阻塞兄弟任务”和“重启后不重复领取”测试。

### 阶段 C：改造循环候选选择

- 将现有固定 resident priority 选择改为“先判断是否可运行，再比较优先级”。
- 增加将领 ID 级锁和候选冲突测试。
- 固定 `打矿 > 刷黄 > 副本` 的回放场景。
- 保持各功能现有 worker/state machine，只把 `run` 的入口交给新选择器。

### 阶段 D：接入闲时独立执行线

- 主线没有可执行每日/循环任务时发放 idle permit。
- 闲时线只执行找黄、找矿、六部、内政/科技、背包动作。
- 主线出现可执行候选时，闲时线在当前网络步骤结束后让步。
- 添加主线/闲时线程竞态、停止账号和设置保存测试。

### 阶段 E：双端一致回放和灰度切换

- 电脑端和 Android 都只调用共享 Python 选择器。
- 用同一组配置、将领状态、目标状态和时间输入，比较两端候选及选择结果。
- 新旧选择器并行做离线 shadow comparison，但真实环境只允许新选择器发包。
- 通过账号级开关灰度启用，确认稳定后再删除旧平铺调度入口。

## 7. 必须通过的验收场景

1. 登录后七类每日任务全部完成/得到终态，之后才开始刷黄、打矿、副本。
2. 同一将领同时满足打矿、刷黄、副本时，顺序严格为打矿 → 刷黄 → 副本。
3. 打矿没有目标时，不报错、不空转，刷黄可以接管。
4. 刷黄达到每日上限或无目标时，副本可以接管。
5. 高优先级任务等待服务器时，不重复发包；其实际占用的将领保持锁定。
6. 闲时找矿进行中突然出现可执行打矿，闲时线在安全边界让步。
7. 闲时任务不得与主线对同一账号同时发游戏请求。
8. 单个每日任务失败，不阻塞其他每日任务，也不制造循环任务重复执行。
9. 进程重启、断网、回执不明后，先恢复账本，不重做已越过发送边界的动作。
10. Android 和电脑端对同一输入快照返回完全相同的任务选择结果。

## 8. 当前结论

这不是重写功能，而是把现有功能从“平铺任务列表”改成“每日门禁 + 循环资源调度 + 闲时让步调度”。第一阶段只调整任务选择和线程归属，功能内部细节全部沿用现有共享 Python 实现。

## 9. 阶段 B 实施记录（2026-08-25）

已完成的每日门禁行为：

- 共享契约 `dailyFeaturesRunBeforeResidents=true`，电脑端和 Android 都强制校验该规则。
- 待决账本恢复仍然先于每日任务，任何已经越过发送边界的动作不会因为门禁改造而重放。
- 每个 tick 最多推进一个日常任务；当前周期仍有到期兄弟任务时，下一 tick 立即继续日常。
- 当前周期日常全部得到确定结论后，才选择刷黄、打矿、副本等常驻任务。
- 单个日常明确失败只保存自己的重试时间；重试时间未到时，常驻任务可以运行，重试时间到达后日常重新优先。
- 普通日常按中国时区 00:00 计算周期，竞技币继续按中国时区 22:00 计算周期，因此中午登录会检查当前周期尚未完成的全部日常。

验证结果：共享 Python 全量 946 项测试通过，Android `testDebugUnitTest` 通过；debug APK 已在真机 `27a83c9c` 覆盖安装并保留原账号及待决账本。安装后的首轮 operation 已按“签到、竞技币、捐献、俸禄、国家征收、城主征收 → 常驻刷黄”顺序运行。
