# 军事功能抓包充分性评估（2026-07-10）

## 已完成：掠夺

- 结论：信息充足，电脑端已接入真实执行。
- 已实现接口：
  - `0x1310/0x8310`：按玩家名查询封地列表，按封地序号解析 `targetId`。
  - `0x1520/0x8520`：掠夺预出征。
  - `0x1522/0x8522`：掠夺正式出征。
- 成功测试：测试账号 `1608601`，将领 `步1 + 骑1`，掠夺 `天雄星` 第 10 个封地 `永原封地` 成功。
- 证据文件：`/Users/huangchangwei/Desktop/gitSpaceC/Toy/帝王三国/电脑端辅助前端/reports/raid_1783638401359.json`

## 无损

- 结论：当前资料不足以安全开发真实执行。
- 目前只看到日常任务文本/字典类信息，例如“参加5轮无损闯关”，没有完整的“进入无损页 → 选择等级 → 发起 → 轮询 → 结算”请求链。
- 已做准备：电脑端页面配置可保存；后端 `/api/military/future/save` 会按账号落库，但不会执行。
- 需要补抓：选择一个无损等级，派将开始，等待至少一次结果返回。

## 副本

- 结论：资料基本足够进入开发。
- 可用样本：`/Users/huangchangwei/Desktop/gitSpaceC/Toy/帝王三国/ctf_out/dungeon_capture_20260706_023009/game_flows.json`
- 已识别链路：
  - `0x1930/0x8930`：副本列表/章节状态。
  - `0x1520/0x8520`：副本预出征。
  - `0x1522/0x8522`：正式启动，响应文本含“单人副本启动成功”。
  - `0x1938/0x8938`：获取副本战斗/状态 ID。
  - `0x1702/0x8702`：战斗轮询。
  - 另有 `0x193d`、`0x193e` 疑似副本后续/开箱相关，需要开发时再细分。
- 已做准备：电脑端副本配置可保存；后端标记为 `protocol_ready`。

## 押镖

- 结论：当前资料不足以安全开发真实执行。
- 目前只看到日常任务文本/字典类信息，例如“参与1次押镖系统”，没有完整的“选择镖车 → 发车 → 状态/撤军/结算”请求链。
- 已做准备：电脑端页面配置可保存；后端 `/api/military/future/save` 会按账号落库，但不会执行。
- 需要补抓：选择镖车品质，派将出发；如能补一次撤军/被劫/完成结算更好。

## 寻宝

- 结论：当前资料不足以安全开发真实执行。
- 目前没有完整的“刷新藏宝图 → 选择宝藏 → 发起寻宝 → 加速/结果”链路。
- 已做准备：电脑端页面配置可保存；后端 `/api/military/future/save` 会按账号落库，但不会执行。
- 需要补抓：刷新藏宝图、选择一个宝藏、派将出发；可选补抓加速和自动购买藏宝图。
